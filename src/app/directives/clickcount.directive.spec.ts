import { ClickcountDirective } from './clickcount.directive';

describe('ClickcountDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickcountDirective();
    expect(directive).toBeTruthy();
  });
});
